# Core pipeline dependencies (CLI). Kept in sync with pyproject.toml.
# Note: BLAST+ (tblastn) is a system dependency, installed via the OS package
# manager (see Dockerfile), not pip.
biopython>=1.81,<1.90   # NOTE: pins below 1.90 because Bio.Blast.NCBIXML is used
tqdm>=4.65,<5
